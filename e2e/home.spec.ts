import { test } from "@playwright/test";

test.describe("Home page", () => {
  test("should show the home page", async ({}) => {
    // TODO: Implement this test when we have the full workflow of the screen.
  });
});
